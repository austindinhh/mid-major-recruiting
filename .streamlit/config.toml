[theme]
primaryColor      = "#FF5F05"   # Illinois orange — buttons, sliders, active states
backgroundColor   = "#FFFFFF"   # white main content area
secondaryBackgroundColor = "#13294B"  # Illinois blue — sidebar
textColor         = "#13294B"   # Illinois blue — body text on white
font              = "sans serif"
