"""
Generates the current-season hidden-gems board.
Requires a trained model and current-season player data in the cache.
Run from the repo root: python scripts/generate_board.py
"""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

# TODO: implement once model training (run_pipeline.py) is complete
print("generate_board.py: not yet implemented — run run_pipeline.py first.")
